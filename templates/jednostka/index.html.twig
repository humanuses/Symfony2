{% extends 'base.html.twig' %}

{% block title %}Hello JednostkaController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

</div>
   <h1>JEDNOSTKA</h1>
    <h2> (dodaj lub edytuj)</h2>
 <form action="{{ path('app_jednostka') }}">
        
            <input
                type="search"
                name="searchvalue"
                class="form-control"
                placeholder="nazwa jednostki"
                aria-label="Search"
            ><button > ZNAJDŹ</button>
        </form>
        {% if JednostkaForm is defined %}
{{ form_start(JednostkaForm) }}
<button type="submit" class="btn">Dodaj jednostkę</button>
{{ form_end(JednostkaForm) }}
{% endif %}
{% endblock %}
